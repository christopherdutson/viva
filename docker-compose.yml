services:

  backend:

    build: ./backend

    ports:
      - "3000:3000"

    environment:
      - PORT=3000
      - DB_PATH=/app/data/app.db

    volumes:
      - ./backend:/app
      - ./backend/data:/app/data

    command: npm run dev
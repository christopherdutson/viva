server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Support Angular HTML5 routing — unknown paths fall back to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
